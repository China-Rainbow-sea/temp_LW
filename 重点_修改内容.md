# 1.2 国内外研究现状
目前，嵌入式蓝牙音箱的硬件架构主要包括单芯片方案、MCU+蓝牙模块方案两种类型。单芯片方案依靠集成蓝牙功能的音频SoC芯片作为主控，如杰理科技的AC系列芯片，具有成本低、集成度高、开发周期短等优点。MCU+蓝牙模块方案依靠通用MCU作为主控，外接独立蓝牙模块让无线通信功能得以实现，灵活性高，便于功能扩展。说到音频处理技术，D类功放具有效率高、发热低、体积小等优势，特别适合便携式音箱应用。说到显示技术，LED点阵动态扫描显示技术因其成本低、驱动简单等特点，被广泛应用于嵌入式设备的视觉反馈系统中。  
尽管蓝牙音箱技术已经相对成熟，但现有产品仍存在一些不足。部分蓝牙音箱在待机状态下功耗较高，影响电池续航时间。受限于成本和体积，入门级蓝牙音箱在音质表现上往往不尽如人意。传统蓝牙音箱多使用物理按键控制，操作方式较为单一。这套方案针对上述问题，使用AC7916单芯片方案，结合LED点阵显示和独立按键控制，在保证低成本的同时获取良好的人机交互体验。

---

# 1.3 设计要求
  
 这个系统以杰理AC7916A蓝牙音频SoC作为主控制单元，外围电路包括音频功率放大模块、LED点阵显示模块、按键输入模块和电源管理模块，实现蓝牙音频播放、播放信息显示和用户交互控制等功能。系统主要由主控模块、音频功放模块、LED显示模块、按键控制模块和电源管理模块构成。具体设计要求如下：  
1、主控模块：使用杰理AC7916A蓝牙音频SoC作为主控芯片，该芯片拥有蓝牙5.0射频、ARM Cortex-M3内核和音频DAC，可以对蓝牙协议栈运行、音频数据处理以及各外设模块的协调控制进行负责，并通过FreeRTOS实时操作系统实现多任务调度管理。  
2、音频功放模块：使用HAA2018A(B)-R音频功率放大器实现音频信号功率放大，该芯片支持AB/D类模式切换，在14V供电时输出功率获得5W以上，芯片内部集成增益设置电阻无需外部配置，差分输出端直接驱动扬声器无需输出耦合电容。  
3、LED显示模块：使用5位8段LED数码管动态扫描显示方案，由38颗0603封装白光LED组成，通过GPIO直接驱动做到音乐剩余时间的实时显示，位选信号由PC1至PC5引脚控制，段选信号由PA6至PA10、PC6、PC10引脚控制，扫描频率设置为100Hz，每位点亮时长2ms，限流电阻取510Ω把工作电流限制在10mA左右。  
4、按键控制模块：设置6个独立按键实现用户交互控制，使用K2-1109SP-H4SA-04型号轻触开关，按键分别接入PC0、PC7、PB1、PA1、PB0、PA4引脚，通过低电平有效触发方式，通过芯片内部上拉电阻实现稳定输入，软件层面通过20ms延时消抖算法确保按键识别准确。  
5、电源管理模块：通过单节锂电池供电方案，由充电管理、电池保护和电压转换三部分组成，充电管理使用TC4056A线性充电芯片实现1A充电电流，电池保护使用XB5306A二合一保护IC实现过充过放保护，3.3V电源通过异步降压DCDC转换，14V电源域用于功放供电。

---

# 2.2.1 硬件架构设计
  
系统硬件整体架构如所示。

图 2-2 AC7916主控模块原理图  
这个系统使用AC7916A蓝牙音频微控制器作为主控核心，构建完整蓝牙音箱播放平台。硬件架构上，AC7916A集成蓝牙功能，直接与智能手机等移动设备建立无线连接，接收音频流与播放控制命令（播放/暂停、切歌、音量调节等）。  
音频处理后，AC7916A通过I2S接口向HAA2018A高效音频功率放大器输出音频与控制信号，驱动扬声器发声。  
用户交互方面，通过6个独立按键连接至MCU通用IO口，实现用户按键控制。系统搭载多组LED点阵，通过动态扫描驱动，用于音乐剩余时间显示。电源系统通过Type-C 16PIN接口输入5V供电，配合TC4056A充电管理、XB5306A电池保护和5V转3.3V DCDC电路，实现蓝牙连接、音频播放、人机交互与视觉反馈一体化。

---

# 2.2.2 模块功能说明
  
该蓝牙音箱系统硬件由主控模块、音频功率放大模块、人机交互模块、电源管理模块四大功能单元构成，各模块分工明确、协同工作，实现蓝牙音频播放、按键控制与状态视觉反馈一体化功能。  
主控模块  
系统使用AC7916A微控制器作为核心控制单元，负责整机逻辑调度与资源管理。芯片集成蓝牙射频与音频处理能力，直接实现无线数据接收、音频解码、播放状态控制、按键扫描与LED驱动等任务，无需外接独立主控芯片，简化硬件结构并提升系统稳定性[8]。  
音频功率放大模块  
通过HAA2018A音频功率放大器实现音频信号放大输出。AC7916A通过I2S接口向功放芯片传输控制与音频信号，HAA2018A将信号进行功率放大后驱动扬声器发声，保证音频输出响度与音质，满足音箱外放需求。  
人机交互模块  
按键控制单元：系统配置6路独立机械按键，通过通用GPIO端口与主控连接，实现播放/暂停、曲目切换、音量调节等基础操作，响应及时、控制稳定。  
LED指示单元：由38颗NCD0603W1型0603封装白光LED组成点阵显示结构，主控通过动态扫描方式驱动，通过动态扫描技术实时显示音乐剩余播放时间，通过直观的视觉形式反馈当前播放进度。  
电源管理模块  
采用Type-C 16PIN接口输入USB 5V供电，CC引脚配置5.1kΩ下拉电阻R40、R41实现USB2.0模式识别。系统通过DCDC降压电路转换为3.3V为核心供电，同时集成TC4056A线性充电管理芯片和XB5306A二合一锂电保护IC，支持单节锂电池供电与充电管理，为主控、功放、交互模块提供稳定供电，保障系统安全可靠运行。



---

# LED 点阵方案
LED点阵方案的优势主要体现在以下几个方面：  
LED点阵的硬件成本最为低廉，这很符合毕业设计项目的预算约束。模块支持PWM调光功能，能够根据环境光照自动调整亮度，确保在不同光线条件下的可视性。通过动态扫描驱动方式，仅需少量GPIO引脚即可控制多组LED，极大节省了宝贵的微控制器I/O资源。点阵式数字显示方式简洁直观，恰好满足这个系统仅需呈现音乐剩余时间这一核心信息的需求，避免了不必要的图形化显示带来的复杂度提升。  
相比之下，LCD1602虽能显示字符但需要额外驱动芯片成本较高，OLED支持丰富图形但价格昂贵，数码管虽成本低但功耗显著偏高。LED点阵方案在成本效益、系统集成度和功能适用性之间取得了最优平衡，是此项目最为合理的技术选择。

---

# 3.2 电源管理模块设计


输入端D2采用SS14肖特基二极管实现防反接保护，C63、C64两颗100nF电容提供高频去耦。  
电池保护选用XB5306A二合一保护IC，集成过充（4.3V切断）、过放（2.4V切断）、过流（3A切断）和短路保护功能，VM引脚检测电池电压，内置功率MOSFET实现回路控制。电池接口采用BX-PH2.0-2PZZ连接器。  
电压转换部分，3.3V电源采用异步降压DCDC，电感L4取2.2μH，反馈电阻R45=10kΩ、R47=220kΩ设定输出电压，C39/C59取1uF作为输入滤波，C66/C67取470nF作为输出滤波。

---

# 3.3 音频功放模块设计


这里选用HAA2018A(B)-R音频功率放大器实现音频信号功率放大，该芯片支持AB/D类模式切换[11]，14V供电时输出功率获得5W以上，满足便携式音箱音量需求。芯片-IN引脚通过耦合电容接收AC7916A的DACL音频信号，内部集成增益设置电阻无需外部配置；SD引脚连接主控GPIO实现待机控制，系统休眠时关闭功放降低功耗；BYPASS引脚外接C36=1uF电容为内部偏置电路提供参考电压。OUTN和OUTP为差分输出端，直接驱动扬声器无需输出耦合电容，简化电路设计。VDD引脚连接14V电源域，GND连接功率地PGND，与信号地单点连接避免地环路干扰。C37=470uF电解电容并联在电源引脚附近，为功放瞬态大电流提供储能，确保大动态输出时电源稳定。  
功放电路设计如图 3-3所示。

---

# 3.4 按键控制模块设计


这里设置6个独立按键实现用户与音箱的交互控制，按键使用K2-1109SP-H4SA-04型号轻触开关，4mm×4mm贴片封装。SW1连接PC0、SW2连接PC7、SW3连接PB1、SW4连接PA1、SW5连接PB0、SW6连接PA4，都通过低电平有效触发方式，按键一端连接GPIO另一端接地。AC7916A内部集成可配置上拉电阻，使能后按键未按下时引脚保持高电平，按下时变为低电平[13]，主控检测下降沿判断按键动作，无需外部上拉电阻简化电路。  
软件层面通过20ms延时消抖算法，检测到按键按下后延时20ms再次检测，确认有效后执行操作避免误触发。按键功能分配为：KEY1上一曲、KEY2下一曲、KEY3音量减小、KEY4音量增大、KEY5播放暂停切换、KEY6蓝牙断开重连。长按和短按通过软件计时区分，短按小于500ms执行单击功能，长按大于500ms执行连续调节，提升操作体验。

---

# 3.5 LED点阵显示模块设计


这里使用5位8段LED数码管动态扫描显示方案，实现播放时间、蓝牙状态等信息显示。显示电路由38颗NCD0603W1白光LED组成，使用0603贴片封装，直接由AC7916A GPIO驱动无需专用驱动芯片。位选信号由PC1至PC5引脚控制，段选信号a至g和小数点dp分别由PA6、PA7、PA8、PA9、PA10、PC6、PC10控制，100Hz频率轮流点亮5个数码管，视觉暂留效应实现稳定显示。限流电阻R52、R53取510Ω，串联在LED回路中将工作电流限制在10mA左右，既保证亮度又避免过流损坏GPIO。  
第5位数码管兼具冒号显示功能，用于时间显示时的分秒分隔，冒号由PC6引脚控制的独立LED实现。充电状态指示通过CHARGE网络实现，连接TC4056A的CHRG引脚，充电时输出低电平点亮LED，充满后转为高阻态熄灭指示。动态扫描频率设置为100Hz，每个数码管点亮2ms，整体刷新周期10ms，显示效果稳定无闪烁。

---

# 4.2 主程序设计
这里的主程序设计基于FreeRTOS实时操作系统，通过多任务架构实现蓝牙音频处理、LED显示刷新和按键扫描的并发执行。系统上电后，app_main函数作为应用程序入口，首先调用bt_ble_module_init函数实现蓝牙协议栈初始化，包括射频校准、服务注册和连接参数配置；随后通过bt_music_info_handle_register函数注册音乐信息回调函数info_cb，用于接收来自手机端的播放时间和总时长数据。  
这里创建了两个核心任务：show_led_task任务负责LED动态扫描显示，优先级设为10，堆栈大小128字节，使用vTaskDelayUntil函数实现精确的10ms周期调度，确保100Hz刷新频率；keyTask任务负责按键扫描与处理，优先级设为20，堆栈大小1024字节，以20ms周期执行按键状态检测。任务调度方面，show_led_task在检测到蓝牙设备连接后，通过get_total_connect_dev函数获取连接状态，依次调用Inf_LED_ShowNum函数刷新5位LED显示内容，每实现一轮扫描后调用Inf_LED_CloseAllNums函数消除拖影，同时每100个周期通过user_send_cmd_prepar函数发送USER_CTRL_AVCTP_OPID_GET_PLAY_TIME命令查询音乐播放时间。keyTask任务则循环调用Inf_Key_Scan函数获取按键状态，根据返回的KEY1至KEY6编码，构造key_event结构体并调用bt_music_key_event_handler函数执行相应的蓝牙音乐控制操作，包括上一曲、下一曲、音量调节、播放暂停和蓝牙断开重连功能。  
主程序负责系统初始化和任务创建，核心代码如下：



---

# 4.3 按键驱动程序设计
  
这里的按键驱动程序通过独立式按键扫描方案，通过GPIO输入检测和软件消抖实现可靠的按键识别。按键初始化在Inf_Key_Init函数中实现，该函数依次配置6个按键对应的GPIO引脚为输入模式，包括IO_PORTB_00、IO_PORTC_00、IO_PORTC_07、IO_PORTB_01、IO_PORTA_04和IO_PORTA_01，分别对应音量加、播放暂停、上一首、下一首、断开重连和音量减功能，每个引脚通过gpio_set_pull_up函数启用内部上拉电阻，确保按键未按下时引脚保持高电平状态。  
按键扫描功能由Inf_Key_Scan函数实现，该函数首先通过gpio_read函数轮询检测6个引脚的电平状态，当检测到任一引脚为低电平时，调用os_time_dly函数延时20ms进行软件消抖，消除机械按键的抖动干扰；20ms后再次检测该引脚电平，若仍为低电平则确认按键有效按下，随后通过while循环阻塞等待按键释放，避免重复触发，最后返回对应的My_Key枚举值KEY1至KEY6。这里通过20ms消抖延时和等待释放机制，确保一次按键操作只触发一次功能执行，消抖后的按键识别准确率显著提升，无连击或误触发现象。



---

# 4.4 LED显示驱动程序设计


本设计的LED显示驱动程序采用动态扫描技术，通过GPIO直接驱动5组LED点阵实现音乐剩余时间的实时显示。显示控制核心在Inf_LED_ShowNum函数中实现，该函数接收group参数指定显示位号（1-5）和num参数指定显示数字（0-9），首先通过gpio_direction_output函数将所有位选引脚PC1至PC5置高电平关闭显示，然后根据group参数将对应位选引脚置低电平选通当前显示位，再通过PA6至PA10、PC6、PC10共7个段选引脚的高低电平组合控制LED段点亮，形成0-9数字的七段码显示，其中第5位用于冒号显示，通过判断num参数值控制PC6引脚实现冒号的点亮与熄灭。动态扫描功能在show_led_task任务中实现，该任务以10ms为周期循环执行，每次依次调用Inf_LED_ShowNum函数显示ledBuff数组中的5位字符，每位显示后通过delay_us函数延时200μs，完成一轮扫描后调用Inf_LED_CloseAllNums函数将所有位选置高关闭显示，利用人眼视觉暂留效应形成稳定的数字显示效果。LED显示内容通过info_cb回调函数更新，该函数在接收到蓝牙音乐时间信息时，计算总时长与当前播放位置的差值得到剩余时间，通过sprintf函数格式化为MM：SS形式存入ledBuff数组，供显示任务读取刷新。

---

# 4.5 蓝牙音乐控制逻辑


KEY5映射为播放暂停切换功能，通过KEY_OK值触发；KEY6实现蓝牙断开重连功能，通过get_bt_connect_status函数获取当前连接状态，若已连接则依次调用user_send_cmd_prepare函数发送USER_CTRL_DISCONNECTION_HCI命令断开当前连接，再发送USER_CTRL_WRITE_SCAN_ENABLE和USER_CTRL_WRITE_CONN_ENABLE命令开启可发现和可连接模式，等待手机重新配对连接。  
音乐时间获取通过info_cb回调函数实现，该函数在type参数为0时接收播放时间信息，分两次调用分别获取总时长和当前播放位置，计算差值后sprintf函数格式化为分秒形式更新ledBuff显示缓冲区，实现剩余播放时间的实时显示。





